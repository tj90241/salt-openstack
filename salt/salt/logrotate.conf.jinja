{%- for log in (['api', 'api_access', 'api_error', 'master', 'minion', 'key'] if 'salt-masters' in pillar.get('nodegroups', []) else ['minion']) %}
/var/log/salt/{{ log }} {
	daily
	missingok
	rotate 7
	compress
	notifempty
}
{% endfor %}
